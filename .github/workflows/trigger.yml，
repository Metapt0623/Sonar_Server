name: 响应联动
on:
  repository_dispatch:
    types: [parent_update] # 匹配上面发出的信号名

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 执行任务
        run: |
          echo "收到 Identity_Node 的信号，开始执行自动化作业..."
          # 这里写你原本的构建或检查代码，例如：npm install && npm test
